import React from "react";
import { Button, Modal, Card } from "react-bootstrap";

function list() {
  return (
  
    <Card className="list">
      <div className="list">
        
        <Card.Body>
         
          <Button> variant="primary" </Button>
        </Card.Body>
      </div>
    </Card>
  );
}

export default React;
// epic fail need to do some soul searching